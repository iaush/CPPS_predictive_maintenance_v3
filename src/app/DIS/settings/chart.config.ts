const chartFontFamily = 'sans-serif';

export const chartConfig = {
  mainTitleFont: `1.3vw ${chartFontFamily}`,
  axisTitleFont: `0.75vw ${chartFontFamily}`,
  axisLabelFont: `0.5vw ${chartFontFamily}`,
  legendFont:  `0.5vw ${chartFontFamily}`,

};
