const chartFontFamily = 'sans-serif';

export const chartConfig = {
  mainTitleFont: `1.3vw ${chartFontFamily}`,
  axisTitleFont: `1vw ${chartFontFamily}`,
  axisLabelFont: `1vw ${chartFontFamily}`,
  legendFont:  `1vw ${chartFontFamily}`,
  markerSize:  15,
  lineWidth: 3,
  majorLineWidth: 1,
  lineChartWidth: 5,
  lineColor: '#9191a1'
};
